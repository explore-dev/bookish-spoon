name: Revy
on: [pull_request]

jobs:
  reviewpad-action:
    name: Reviewpad action
    runs-on: ubuntu-latest
    steps:
      - name: Runs reviewpad action
        uses: explore-dev/cautious-broccoli@experimental
